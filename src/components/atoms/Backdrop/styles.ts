import styled from "styled-components";

//Here the screen is properly styled
export const Screen = styled.div`
    height: 100%;
    width: 100%;
    background: rgba(0, 0, 0, 0.3);
    position: fixed;
    top: 0;
    left: 0;
`;
